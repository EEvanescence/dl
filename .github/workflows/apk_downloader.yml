name: APK Downloader

on:
  workflow_dispatch:
    inputs:
      package_name:
        description: 'Package name (e.g., org.telegram.messenger)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  download-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download apkeep
        run: |
          # Fetch the latest stable release for Linux (GNU)
          wget -q https://github.com/EFForg/apkeep/releases/latest/download/apkeep-x86_64-unknown-linux-gnu -O apkeep
          chmod +x apkeep

      - name: Fetch APK
        run: |
          mkdir -p downloads
          ./apkeep -a ${{ inputs.package_name }} -d apk-pure downloads/

      - name: Process and Split
        run: |
          cd downloads
          DOWNLOADED_FILE=$(ls -1A | head -n 1)
          
          if [ -z "$DOWNLOADED_FILE" ]; then
            echo "Error: Downloaded file not found."
            exit 1
          fi

          7z a -mx=3 -v85m "${{ inputs.package_name }}.7z" "$DOWNLOADED_FILE"
          rm "$DOWNLOADED_FILE"


      - name: Commit and Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add downloads/
          git commit -m "Add ${{ inputs.package_name }}" || echo "No changes to commit"
          git push
